{
  "name": "fem-time-tracking-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:sass": "sass  --no-source-map sass:public/css",
    "copy:html": "copyfiles -u 1 ./src/*.html public"
  },
  "devDependencies": {
    "sass": "^1.54.5",
    "vite": "^3.0.7"
  }
}
